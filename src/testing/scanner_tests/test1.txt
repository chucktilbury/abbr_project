
/*
    This is a general test
*/
namespace some_name {
    class class_name (public asdf.qwer.kjsd, private qweoiu) {
        create(int something) { do.something().radical() }
        boolean clean // single line
        // another one
        bacon.and.eggs goobers += 123+(8+12)/0.123e-12
        string goob = "this is a\nstring"
        string plart = 'this is a\ndifferent string'
    }
}

nothing some_name.create() {

    x.y.z()
    int blart = 123 + 87 / 5
    inline {
        // this text is copied without changes
        int x = 10;
        int y = 123;
        printf("%d, %d\n);
    }
}

